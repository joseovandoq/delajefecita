# Panel de Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Panel de Supabase → Project Settings → Database → Connection string (modo "Transaction" o "Session")
# Usa el connection string con el pooler (puerto 6543) para producción/serverless.
DATABASE_URL=
